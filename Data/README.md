## Data

This folder contains datasets used to train and evaluate the GNN surrogate models.

Each CSV includes:
- accuracy parameters
- measured numerical error
- CPU runtime

Datasets were generated using the pipeline utilities provided in the `pipeline/` and `notebooks/` directories.

